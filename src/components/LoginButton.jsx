import { Link } from "react-router-dom";

const LoginButton = () => {
  return (
    <div>
      <button>
        <Link to="Forms.jsx">Test</Link>
      </button>
    </div>
  );
};

export default LoginButton;
